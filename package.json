{
  "name": "moviething",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "start": "npm run start --workspace=server",
    "dev": "npm run dev --workspace=server",
    "test": "npm run test --workspaces"
  },
  "devDependencies": {},
  "dependencies": {}
}
